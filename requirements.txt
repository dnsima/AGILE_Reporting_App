# --- Core web framework -------------------------------------------------
fastapi>=0.110,<1.0
uvicorn[standard]>=0.27
python-multipart>=0.0.9

# --- Data layer ---------------------------------------------------------
SQLAlchemy>=2.0,<3.0
pydantic>=2.6,<3.0
pydantic-settings>=2.2
email-validator>=2.1

# --- Ingestion ----------------------------------------------------------
pandas>=2.0
openpyxl>=3.1

# --- Security -----------------------------------------------------------
bcrypt>=4.0

# --- Templating / reporting --------------------------------------------
Jinja2>=3.1

# --- Optional: PDF export (HTML/Markdown work without it) --------------
reportlab>=4.0

# --- Optional: Word export for the NPCU quarterly reports --------------
python-docx>=1.1

# --- Optional: figures for the quarterly technical report --------------
matplotlib>=3.8

# --- Testing ------------------------------------------------------------
pytest>=8.0
httpx>=0.27
